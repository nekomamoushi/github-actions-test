name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: oneline script
        run: echo "Hello Github Actions"
      - name: multiline script
        run: |
          node -v
          npm -v
